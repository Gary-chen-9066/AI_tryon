[project]
name = "ai-tryon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Gary" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
]

[project.scripts]
ai-tryon = "ai_tryon:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
